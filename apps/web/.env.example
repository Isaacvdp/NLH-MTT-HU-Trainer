# Copy to .env.local and fill in. Both values are public, client-side values.
# The Supabase service role key must never appear here — it lives only in
# Edge Function secrets (`supabase secrets set`).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
